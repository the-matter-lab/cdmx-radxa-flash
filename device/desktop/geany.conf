[geany]
# Keep workshop Python indentation deterministic. Geany's auto-detection takes
# precedence over filetype defaults, so both detection switches stay disabled.
pref_editor_tab_width=4
indent_type=0
check_detect_indent=false
detect_indent_width=false
use_tab_to_indent=true
pref_editor_replace_tabs=true
